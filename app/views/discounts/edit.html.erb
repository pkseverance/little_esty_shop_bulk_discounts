<%= render partial: 'form', locals: {method: :patch, url: merchant_discount_path(@merchant, @discount), percent_value: @discount.percent_discount, quantity_value: @discount.quantity_threshold, local: true} %>

